spring:
  main:
    banner-mode: off
  jpa:
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 3001
